import express from 'express';
const router = express.Router();

// Ticket Overview
router.get('/tickets-overview', );

router.get('/user-stats', );

router.get('/customer-satisfaction', );

router.get('/recent-tickets', );

router.get('/recent-activity', );







router.get('/performance-metrics?from=2024-05-01&to=2024-05-08', );

router.get('/agents-performance?range=last7days', );

export default router;
